{
  "name": "portfolio-hub",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview --host 127.0.0.1 --port 8912",
    "gate:links": "node scripts/check-links.mjs dist",
    "gate:ui": "node scripts/gate.mjs http://127.0.0.1:8912"
  },
  "dependencies": {
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
